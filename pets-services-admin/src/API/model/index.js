const loginModel = {
    password: String,
    email: String,
  };

  const searchUserModel = {
    name: String,
    email: String,
    locality: String,
  };

  export { loginModel,  searchUserModel};